# Compute Shader

## First Compute Shader
- kernel
- numthreads & thread groups
- Wacław Sierpiński

## Multiple Kernels
- bitwise operator
- step: `step(edge, x) returns (x >= edge) ? 1 : 0`
- length